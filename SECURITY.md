# Security Policy

Report sensitive vulnerabilities privately through GitHub's security advisory flow when available. Do not paste private pull-request bodies, credentials, or repository contents into public issues.

The supported branch is the latest release line.

Maintainer Contracts performs no network requests and does not execute repository code. It may invoke local `git` commands in a repository selected by the caller. Run it only in CI workspaces and repositories you intend to inspect.
